# Web框架核心
Flask~=3.1.0
Werkzeug~=3.1.3  # WSGI工具库

# 配置管理
config~=0.5.1

# 数据库相关
Flask-SQLAlchemy~=3.1.1  # ORM扩展

# 用户认证
Flask-Login~=0.6.3  # 用户会话管理

# 表单处理
Flask-WTF~=1.2.1  # 表单集成
WTForms~=3.1.1  # 表单验证

# 环境变量
python-dotenv~=1.0.0  # .env文件支持

# 应用打包
PyInstaller~=6.6.0  # 生成可执行文件